{
  "name": "goodwin-bookings",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "predeploy-clean": "rimraf dist && make-dir dist",
    "predeploy-copy": "copyfiles --up 2 src/www/**/* dist",
    "predeploy": "run-s predeploy-clean predeploy-copy",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request-promise-native": "^1.0.7"
  },
  "devDependencies": {
    "copyfiles": "^2.1.0",
    "gh-pages": "^2.0.1",
    "make-dir-cli": "^2.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.3"
  }
}
