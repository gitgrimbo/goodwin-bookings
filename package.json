{
  "name": "goodwin-bookings",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "predeploy:web-clean": "rimraf dist && make-dir dist",
    "predeploy:web-copy": "copyfiles --up 2 src/www/**/* dist",
    "predeploy:web": "run-s predeploy:web-clean predeploy:web-copy",
    "deploy:web": "gh-pages -d dist",
    "deploy:sls": "sls deploy",
    "deploy": "run-s deploy:web deploy:sls",
    "test": "mocha **/*.spec.js",
    "test:watch": "mocha --watch **/*.spec.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "copyfiles": "^2.1.0",
    "gh-pages": "^2.0.1",
    "make-dir-cli": "^2.0.0",
    "mocha": "^6.0.2",
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.3"
  }
}
